---
import SmallMovieCard from './SmallMovieCard.astro';
import { getImageUrl } from '../../lib/movieService';
import { useTranslations } from '../../lib/useTranslations';

interface Props {
  title: string;
  movies: any[];
  viewAllLink?: string;
}

const { title, movies, viewAllLink } = Astro.props;

// Get translations
const langCookie = Astro.cookies.get('language');
const { translations } = useTranslations(langCookie?.value);
---

<section class="small-carousel-section">
  <div class="container">
    <div class="carousel-header">
      <h2 class="carousel-title">{title}</h2>
      <div class="carousel-actions">
        {viewAllLink && (
          <a href={viewAllLink} class="view-all">
            {translations.common.viewAll}
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
        )}
        <div class="carousel-nav">
          <button class="nav-btn nav-prev" aria-label="Previous">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M15 18l-6-6 6-6"/>
            </svg>
          </button>
          <button class="nav-btn nav-next" aria-label="Next">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 18l6-6-6-6"/>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div class="carousel-track small-track">
      {
        movies.map((movie: any) => (
          <SmallMovieCard
            id={movie.slug}
            title={movie.origin_name || movie.name}
            titleVi={movie.name}
            poster={getImageUrl(movie.poster_url)}
            year={movie.year}
            rating={8.0 + Math.random() * 1.5}
            description={movie.content || ''}
          />
        ))
      }
    </div>
  </div>
</section>

<style>
  .small-carousel-section {
    padding: var(--spacing-lg) 0;
  }

  .carousel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--spacing-md);
    gap: var(--spacing-md);
  }

  .carousel-title {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
  }

  .carousel-actions {
    display: flex;
    align-items: center;
    gap: var(--spacing-lg);
  }

  .view-all {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    transition: color var(--transition-fast);
  }

  .view-all:hover {
    color: var(--color-primary);
  }

  .carousel-nav {
    display: flex;
    gap: var(--spacing-xs);
  }

  .nav-btn {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    color: var(--color-text-secondary);
    transition: all var(--transition-fast);
  }

  .nav-btn:hover {
    background: var(--color-surface-hover);
    color: var(--color-text-primary);
  }

  .small-track {
    display: flex;
    gap: var(--spacing-sm);
    overflow-x: auto;
    scroll-behavior: smooth;
    scrollbar-width: none;
    padding-bottom: var(--spacing-sm);
  }

  .small-track::-webkit-scrollbar {
    display: none;
  }

  .small-track > :global(*) {
    flex: 0 0 calc((100% - 6 * var(--spacing-sm)) / 7);
    min-width: 100px;
    max-width: 130px;
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .small-track > :global(*) {
      flex: 0 0 calc((100% - 5 * var(--spacing-sm)) / 6);
    }
  }

  @media (max-width: 992px) {
    .small-track > :global(*) {
      flex: 0 0 calc((100% - 4 * var(--spacing-sm)) / 5);
    }
  }

  @media (max-width: 768px) {
    .small-track > :global(*) {
      flex: 0 0 calc((100% - 3 * var(--spacing-sm)) / 4);
      min-width: 90px;
    }
  }

  @media (max-width: 480px) {
    .small-track > :global(*) {
      flex: 0 0 calc((100% - 2 * var(--spacing-sm)) / 3);
      min-width: 80px;
    }

    .view-all {
      display: none;
    }
  }
</style>

<script>
  document.querySelectorAll('.small-carousel-section').forEach(section => {
    const track = section.querySelector('.small-track') as HTMLElement;
    const prevBtn = section.querySelector('.nav-prev');
    const nextBtn = section.querySelector('.nav-next');
    
    const scrollAmount = 200;

    prevBtn?.addEventListener('click', () => {
      track?.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
    });

    nextBtn?.addEventListener('click', () => {
      track?.scrollBy({ left: scrollAmount, behavior: 'smooth' });
    });
  });
</script>
