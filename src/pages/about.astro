---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';

// Get translations
import { useTranslations } from '../lib/useTranslations';
const langCookie = Astro.cookies.get('language');
const { translations } = useTranslations(langCookie?.value);
---

<BaseLayout title={`${translations.nav.about} - Movie247`}>
  <Header />
  
  <main class="page-content">
    <div class="container">
      <div class="about-wrapper">
        <!-- Hero Section -->
        <section class="about-hero">
          <h1>{translations.about.title}</h1>
          <p class="subtitle">{translations.about.subtitle}</p>
        </section>

        <!-- Mission -->
        <section class="about-section">
          <h2>{translations.about.mission}</h2>
          <p>
            {translations.about.missionText}
          </p>
        </section>

        <!-- Features -->
        <section class="about-section">
          <h2>{translations.about.features}</h2>
          <div class="features-grid">
            <div class="feature-card">
              <div class="feature-icon">🎬</div>
              <h3>Thư Viện Đa Dạng</h3>
              <p>Hàng nghìn bộ phim từ Hollywood đến Châu Á, cập nhật liên tục.</p>
            </div>
            <div class="feature-card">
              <div class="feature-icon">📱</div>
              <h3>Xem Mọi Nơi</h3>
              <p>Hỗ trợ đa nền tảng: Web, Mobile, Smart TV.</p>
            </div>
            <div class="feature-card">
              <div class="feature-icon">🎯</div>
              <h3>Chất Lượng Cao</h3>
              <p>Phát video 4K HDR với âm thanh Dolby Atmos.</p>
            </div>
            <div class="feature-card">
              <div class="feature-icon">💬</div>
              <h3>Phụ Đề Việt</h3>
              <p>Phụ đề tiếng Việt chất lượng cho mọi nội dung.</p>
            </div>
          </div>
        </section>

        <!-- Stats -->
        <section class="about-section">
          <h2>Con Số Ấn Tượng</h2>
          <div class="stats-grid">
            <div class="stat-item">
              <span class="stat-number">10M+</span>
              <span class="stat-label">Người dùng</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">50K+</span>
              <span class="stat-label">Phim & Series</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">4K</span>
              <span class="stat-label">Chất lượng tối đa</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">24/7</span>
              <span class="stat-label">Hỗ trợ</span>
            </div>
          </div>
        </section>

        <!-- Contact -->
        <section class="about-section">
          <h2>{translations.about.contact}</h2>
          <p>Mọi thắc mắc xin vui lòng liên hệ:</p>
          <ul class="contact-list">
            <li>📧 Email: support@saintstream.com</li>
            <li>📞 Hotline: 1900-xxxx-xx</li>
            <li>📍 Địa chỉ: Quận 1, TP. Hồ Chí Minh, Việt Nam</li>
          </ul>
        </section>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>

<style>
  .page-content {
    padding-top: calc(var(--header-height) + var(--spacing-2xl));
    min-height: 100vh;
  }

  .about-wrapper {
    max-width: 900px;
    margin: 0 auto;
    padding-bottom: var(--spacing-3xl);
  }

  /* Hero */
  .about-hero {
    text-align: center;
    margin-bottom: var(--spacing-3xl);
  }

  .about-hero h1 {
    font-size: var(--font-size-4xl);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--spacing-sm);
  }

  .about-hero .subtitle {
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
  }

  /* Sections */
  .about-section {
    margin-bottom: var(--spacing-3xl);
  }

  .about-section h2 {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--spacing-lg);
    color: var(--color-primary);
  }

  .about-section p {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: 1.8;
  }

  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-lg);
  }

  .feature-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--spacing-xl);
    text-align: center;
    transition: border-color var(--transition-fast);
  }

  .feature-card:hover {
    border-color: var(--color-primary);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: var(--spacing-md);
  }

  .feature-card h3 {
    font-size: var(--font-size-md);
    font-weight: var(--font-weight-semibold);
    margin-bottom: var(--spacing-sm);
  }

  .feature-card p {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--spacing-lg);
    text-align: center;
  }

  .stat-item {
    padding: var(--spacing-lg);
  }

  .stat-number {
    display: block;
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    margin-bottom: var(--spacing-xs);
  }

  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  /* Contact */
  .contact-list {
    margin-top: var(--spacing-md);
  }

  .contact-list li {
    padding: var(--spacing-sm) 0;
    color: var(--color-text-secondary);
    font-size: var(--font-size-base);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .about-hero h1 {
      font-size: var(--font-size-2xl);
    }
  }
</style>
